# 🛠️ Low-Level Design (LLD)

Details modular components inside the `@pgos` monorepo:
- **Context Engine**: Universal AST parser parsing modules and dependencies.
- **Semantic Git**: Prompt tracking overlay on git commits.
- **Recovery Engine**: Point-in-time state managers.
- **Validation Engine**: Complete AST-checks and stubs auditors.

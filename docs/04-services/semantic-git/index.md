# 🔄 Semantic Git

An intelligent Git layer associating prompt chains, validations, and tests directly inside commits.
- **Core Commands**: `guardian commit`, `guardian diff`, `guardian rollback`, `guardian restore`.
- **Tracks**: Code, prompts, AI sessions, validation scores, and rollback checkpoints.
